# Canonical API keys for all v1 apps (admin, sales, monitor, order).
# Copy to secrets.properties (gitignored) in this folder only.
# iOS Secrets.xcconfig is auto-synced from this file by scripts/sync_admin_secrets.sh.
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
