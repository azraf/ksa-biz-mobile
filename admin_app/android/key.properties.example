# Copy to key.properties (gitignored) for release APK signing.
# Use the SAME keystore for every release of this app so users can upgrade in place.
storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=your_key_alias
storeFile=../release-keystore.jks
