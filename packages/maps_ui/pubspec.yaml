name: maps_ui
description: Shared Google Maps UI for KSA Biz mobile apps.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  geolocator: ^13.0.2
  google_maps_flutter: ^2.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
