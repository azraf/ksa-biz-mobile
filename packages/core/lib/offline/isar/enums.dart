enum SyncEntityType { order, expense, watchlist, diary }

enum SyncOperation { create, update, delete, payment, cancel }

enum SyncStatus { pending, syncing, done, failed, dead }

enum MediaUploadStatus { pending, uploading, done, failed }
